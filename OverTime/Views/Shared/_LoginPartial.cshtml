@if (Session["OT_Authen"] != null)
{
    <form id="formLogout" name="formLogout" class="navbar-form pull-right" action="@Url.Action("Logout","Home")" method="post">
        <a class="username"><i class="icon-user"></i> @Session["OT_Name"].ToString()</a>
        <button type="submit" class="btn btn-primary"><i class="icon-off icon-white"></i> Log out</button>
    </form>
}
